import Feed from "../../components/Feed";

const Home = () => {
  return (
    <section className="container main-container">
      <Feed userId={null} />
    </section>
  );
};

export default Home;
